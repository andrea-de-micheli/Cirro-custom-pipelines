/*
 * Nextflow config for valinor-cellranger
 */

nextflow.enable.dsl = 2

params {
    multi_config        = null
    feature_reference   = null
    gex_reference_url   = 'https://cf.10xgenomics.com/supp/cell-exp/refdata-gex-GRCh38-2024-A.tar.gz'
    run_id              = null
    fastq_pattern       = '*.fastq.gz'
    create_bam          = false
    cpus                = 16
    memory_gb           = 128
    disk_gb             = 2000
    input_dir           = null
    outdir              = 'results'
}

process {
    cpus   = 4
    memory = '16 GB'
}

docker {
    enabled = true
}

singularity {
    enabled = false
}

manifest {
    name            = 'valinor-cellranger'
    author          = 'Andrea De Micheli'
    description     = 'Run cellranger multi (GEX + CITE-seq + HTO demux) on AWS Batch via Cirro'
    version         = '0.1.0'
    nextflowVersion = '>=22.10.0'
}

timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/timeline.html"
}

report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/report.html"
}

trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/trace.txt"
}
